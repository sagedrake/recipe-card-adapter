## Starting up App:

The first time around, need to run "npm install" in /, /client, and /backend

backend:

- start SQL server
- cd into /backend
- npm run start

client:

- cd into /client
- npm run dev
